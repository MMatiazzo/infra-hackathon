name: Terraform Create Cluster Workflow

on:
  push:
    branches:
      - master
    paths:
      - 'eks/**'
  pull_request:
    branches:
      - master
    paths:
      - 'eks/**'
  workflow_dispatch:

jobs:
  print_success:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Print Success Message
        run: echo "Successful execution!"
